diff a/Assets/Scripts/Weapon/DestroyWeapon.cs b/Assets/Scripts/Weapon/DestroyWeapon.cs	(rejected hunks)
@@ -31,7 +31,15 @@
         {
             _destroyCD++;
         }
-        else if (_destroyCD >= 50 || _destroyNow)
+        else if (_destroyCD >= 50)
+        {
+            Destroy(gameObject);
+        }
+        if(_destroyNowCD < 1 && _destroyNow)
+        {
+            _destroyNowCD++;
+        }
+        else if(_destroyNowCD >= 1)
         {
             Destroy(gameObject);
         }
